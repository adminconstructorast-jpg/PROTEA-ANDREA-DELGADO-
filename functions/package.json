{
  "name": "@protea/functions",
  "version": "0.1.0",
  "private": true,
  "description": "Cloud Functions de PROTEA: WhatsApp RSVP, CRM, CFDI 4.0, pagos e IA",
  "type": "module",
  "main": "lib/functions/src/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.1.1",
    "pdfkit": "^0.19.1",
    "stripe": "^17.5.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/pdfkit": "^0.17.6",
    "typescript": "^5.6.3"
  }
}
